en:
  errors:
    messages:
      end_before_start: "must be after the start date."
  date:
    formats:
      default: "%Y-%m-%d"
      long: "%B %-d, %Y"
      short: "%b %d"
  time:
    formats:
      default: "%a, %d %b %Y %H:%M:%S %z"
      long: "%B %d, %Y %H:%M"
      short: "%d %b %H:%M"
